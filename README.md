# Google Design System

## Execute it on the web

## start:

```shell
melos bootstrap
```

## run:

```shell
cd apps/google_drive
flutter run -d chrome
```

## run:

```shell
cd apps/google_mail
flutter run -d chrome
```

## run:

```shell
cd apps/google_keep
flutter run -d chrome
```
